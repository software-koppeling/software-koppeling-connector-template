# {{ package_name }}

{{ package_description }}

## Prerequisites

- Python 3.12+
- [uv](https://github.com/astral-sh/uv)

## Install dependencies

```bash
uv sync
```

## Run tests

```bash
uv run pytest
```

## GitHub Actions

The repository includes a workflow at `.github/workflows/docker.yml` that:

- builds a Docker image
- pushes to GHCR on `main` and tags starting with `v`
- tags `acceptatie` on `main`
- tags `prod` and the version on releases

## Update from template

```bash
uv tool run copier update
```

## Recreate from template

```bash
uv tool run copier copy --trust --overwrite https://github.com/software-koppeling/software-koppeling-connector-template .
```
